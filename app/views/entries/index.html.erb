<h1><%= @project.name %></h1>
<ul>
  <% @entries.each do |entry| %>
    <li>
      <%= entry.hours %> h
      <%= entry.minutes %>m
      <%= entry.date %>
    </li>
    <%end%>
</ul>

<!-- @current_month_hours lo definimos en el controlador del proyecto-->
<p> The total hours worked this month is: <%= @current_month_hours %></p>